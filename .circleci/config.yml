version: 2
jobs:
  build:
    machine:
      image: circleci/classic:edge
    steps:
      - checkout
      - run:
          name: docker-compose build
          command: docker-compose build
  deploy:
    machine:
      image: circleci/classic:edge
    steps:
      - checkout
      - run:
          name: "build docker image"
          # registry.heroku.com/${HEROKU_APP_NAME}/webは変更することは出来ません．
          # 上記のコメントの通りやとうまくいかない。${HEROKU_APP_NAME}がCircleCI側で読み込まれていない
          command: docker build --rm=false -t registry.heroku.com/practice-react-ryubb/web .
      - run:
          name: setup heroku command
          # CircleCIからHerokuへデプロイするためのスクリプトを実行
          command: bash .circleci/setup_heroku.sh
      - run:
          name: heroku maintenance on
          # Herokuのアプリをメンテナンス中にする
          command: heroku maintenance:on --app practice-react-ryubb
      - run:
          # HEROKU_AUTH_TOKEN is generated by `heroku auth:token`
          name: "push container to registry.heroku.com"
          # Herokuへログインし，作成したイメージをPush,Releaseします．
          command: |
            docker login --username=_ --password=$HEROKU_AUTH_TOKEN registry.heroku.com
            docker push registry.heroku.com/practice-react-ryubb/web
            heroku container:push web --app practice-react-ryubb
            heroku container:release web --app practice-react-ryubb
      - run:
          name: heroku maintenance off
          # メンテナンスモードをオフにします．
          command: heroku maintenance:off --app practice-react-ryubb
workflows:
  version: 2
  build_and_deploy:
    jobs:
      # - build
      - deploy:
          # requires:
          #   - build
          filters:
            branches:
              only: master
